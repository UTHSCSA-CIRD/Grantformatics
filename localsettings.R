## Sergio settings
pfa_label <- c("disparities","dissemination", "methods","options", "systems") # where each fileid == category label
pathname <-  "C:\\Users\\sergan\\Documents\\barepcori" #enter path of abstracts
sepchar <- "\\"

## Alex settings
#pfa_label <- c("disp","diss","meth","options","systems");
#pathname <-  "."; #enter path of abstracts (I keep mine in the local dir)
#sepchar <- "/";

## Common settings
enc <- 'UTF-8';
